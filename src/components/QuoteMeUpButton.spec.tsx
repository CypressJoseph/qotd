import { render } from '@testing-library/react';
import QuoteMeUpButton from './QuoteMeUpButton';

describe('QuoteMeUpButton', () => {
  test.todo('causes a quote to appear')
  //it('causes a quote to appear on the page', () => {
  //  const { getByText, click } = render(<QuoteMeUpButton />);
  //  const btnElement = getByText(/learn react/i);
  //  expect(btnElement).toBeInTheDocument();
  //  fail('implement me!')
  //})
})
